#!/bin/bash

if [ ! -d src ];
then 
    echo 'please run in the jmxxymon directory'
fi

mkdir -p build/java
javac -cp lib/antlr-3.2.jar -d build/java $(find src/java -type f -name \*.java)

(cd build/java; jar tf ../jmxxymon.jar .)

